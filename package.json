{
  "author": "Patrick Song <32438365+patricksongzy@users.noreply.github.com>",
  "name": "sonors",
  "version": "0.1.0",
  "scripts": {
    "build": "rimraf dist pkg && wasm-pack build && webpack",
    "start": "rimraf dist pkg && wasm-pack build && webpack-dev-server --open -d",
    "test": "cargo test && wasm-pack test --headless"
  },
  "devDependencies": {
    "@wasm-tool/wasm-pack-plugin": "^1.1.0",
    "copy-webpack-plugin": "^5.0.3",
    "webpack": "^4.42.0",
    "webpack-cli": "^3.3.3",
    "webpack-dev-server": "^3.7.1",
    "rimraf": "^3.0.0"
  }
}
